import { CommonModule } from '@angular/common';
import { RouterOutlet } from '@angular/router';
import { ComponentImport } from '@biz-away/core';

export const APP_IMPORTS: ComponentImport[] = [CommonModule, RouterOutlet];
